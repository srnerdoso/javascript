function verification(test) {
  if (test) {

  } else {
    // let _check: never

    // let text = _check

    /* text = '' // O tipo '""' não pode ser atribuído ao tipo 'never'. */

    // return _check
  }
}